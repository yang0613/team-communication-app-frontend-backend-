import React from 'react';

/**
 * @return {Channels}
 */
function Channels() {
  // const user = JSON.parse(localStorage.getItem('user'));

  return (
    <div>
    </div>
  );
}

export default Channels;
